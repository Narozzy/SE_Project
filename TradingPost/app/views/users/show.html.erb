<p id="notice"><%= notice %></p>
<% @user = User.find(params[:id]) %>
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Major:</strong>
  <%= @user.major %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %>
<%= link_to 'Back', users_path %>
